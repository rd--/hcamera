hcamera
-------

Trivial EXIF image file tool.

To generate metadata files for MP4 videos type:

~~~~
exiftool -globalTimeShift +11:00 -S -w meta *.mp4
~~~~

Indirectly requires libexif:

    sudo apt-get install libexif-dev

rd, 2008-2017
